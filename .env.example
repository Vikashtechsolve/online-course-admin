# Copy to .env and adjust. Do not commit .env.
#
# Base URL for the online-course-backend API (must end with /api).
# Must match backend PORT in online-course-backend/.env (default there is 8000).

VITE_API_URL=http://localhost:8000/api
